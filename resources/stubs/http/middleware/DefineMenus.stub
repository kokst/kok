<?php

namespace Modules\$STUDLY_NAME$\Http\Middleware;

use Closure;
use Lavary\Menu\Facade as Menu;

class DefineMenus
{
    public function handle($request, Closure $next)
    {
        $menu = Menu::get('primary') ?? Menu::make('primary', function () {
        });

        $menu->add(strval(__('$LOWER_NAME$::index.title')), ['action' => '\Modules\$STUDLY_NAME$\Http\Controllers\$STUDLY_NAME$Controller@index']);

        return $next($request);
    }
}
